<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendance_assistor"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="23" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name">
        <activity
            android:name=".Main_Page"
            android:label="@string/app_name" >
           <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>  
        </activity>
        <activity
             android:name=".Subject_List"
            android:label="@string/app_name" />
        <activity
             android:name=".subject_info"
            android:label="@string/app_name" />
         <activity
             android:name=".DbHelper_subject"
            android:label="@string/app_name" />
         <activity
             android:name=".DbHelper_student"
            android:label="@string/app_name" />
         <activity
             android:name=".student_info"
            android:label="@string/app_name" />
         <activity
             android:name=".Student_List"
            android:label="@string/app_name" >
            
            
           </activity>
            <activity
             android:name=".delete_subject_list"
            android:label="@string/app_name" />
             <activity
             android:name=".delete_subject"
            android:label="@string/app_name" />
              <activity
             android:name=".display_attendance"
            android:label="@string/app_name" />
               <activity
             android:name=".Calendar_View"
            android:label="@string/app_name" />
                <activity
             android:name=".database"
            android:label="@string/app_name" />
                 <activity
             android:name=".Date_Wise"
            android:label="@string/app_name" />
    </application>

</manifest>
